{
  "name": "marketplace",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ Marketplace",
    "create-local": "graph create --node http://localhost:8020/ trevorgames/marketplace-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ trevorgames/marketplace-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 trevorgames/marketplace-sepolia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.2",
    "@graphprotocol/graph-ts": "^0.34.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
